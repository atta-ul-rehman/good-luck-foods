# Change Review Log

The automated incremental review log is stored in `.github/reviews/local-change-review.md`.

That file is intentionally ignored by git so the repository does not become dirty after every commit. Future chat sessions should read it after `codebase-review.md`.
